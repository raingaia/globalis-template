<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GLOBALIS • Corporate Consulting Template</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">

    <style>
        /* --- 1. CORE VARIABLES & RESET --- */
        :root {
            --bg-body: #f8f9fa;
            --bg-card: #ffffff;
            --text-primary: #111827;
            --text-secondary: #4b5563;
            --text-light: #9ca3af;
            --gold-accent: #c5a059;
            --font-serif: 'Playfair Display', serif;
            --font-sans: 'Inter', sans-serif;
            --container-width: 1200px;
            --header-height: 80px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        
        html, body {
            width: 100%;
            overflow-x: hidden;
            background-color: var(--bg-body);
            color: var(--text-primary);
            font-family: var(--font-sans);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        /* --- 2. FLUID BACKGROUND --- */
        .fluid-bg {
            position: fixed;
            top: 0; left: 0; width: 100vw; height: 100vh;
            z-index: -1;
            pointer-events: none;
            background: 
                radial-gradient(at 0% 0%, rgba(200, 200, 200, 0.15) 0px, transparent 50%),
                radial-gradient(at 100% 0%, rgba(180, 180, 180, 0.1) 0px, transparent 50%),
                radial-gradient(at 100% 100%, rgba(197, 160, 89, 0.05) 0px, transparent 50%);
            filter: blur(80px);
            opacity: 0.8;
            animation: fluidMove 20s infinite alternate;
        }

        @keyframes fluidMove {
            0% { transform: scale(1); }
            100% { transform: scale(1.05); }
        }

        /* --- 3. SCROLL REVEAL --- */
        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 1s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .reveal.active { opacity: 1; transform: translateY(0); }

        /* --- 4. HEADER --- */
        header {
            position: fixed;
            top: 0; left: 0; width: 100%;
            height: var(--header-height);
            padding: 0 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(248, 249, 250, 0.9);
            backdrop-filter: blur(15px);
            border-bottom: 1px solid rgba(0,0,0,0.05);
            z-index: 1000;
            transition: all 0.3s ease;
        }
        
        header.scrolled { 
            height: 70px; 
            background: rgba(255, 255, 255, 0.95);
        }

        .brand {
            font-family: var(--font-serif);
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--text-primary);
            letter-spacing: -0.5px;
            text-transform: uppercase;
            flex-shrink: 0;
        }

        nav { display: flex; gap: 30px; }
        nav a {
            color: var(--text-secondary);
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: color 0.3s;
            position: relative;
        }
        nav a:hover { color: var(--text-primary); }
        nav a::after {
            content: ''; position: absolute; bottom: -4px; left: 0; width: 0; height: 1px;
            background: var(--text-primary); transition: width 0.3s;
        }
        nav a:hover::after { width: 100%; }

        .btn-header {
            border: 1px solid var(--text-primary);
            padding: 10px 20px;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 600;
            color: var(--text-primary);
            background: transparent;
            cursor: pointer;
            transition: 0.3s;
            white-space: nowrap;
            flex-shrink: 0;
        }
        .btn-header:hover {
            background: var(--text-primary);
            color: white;
        }

        /* --- 5. HERO --- */
        .hero {
            min-height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 120px 20px 60px;
            max-width: 1000px;
            margin: 0 auto;
        }

        .eyebrow {
            color: var(--gold-accent);
            font-size: 0.8rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 24px;
            display: block;
        }

        .hero h1 {
            font-family: var(--font-serif);
            font-size: clamp(3rem, 5vw, 5.5rem);
            line-height: 1.1;
            color: var(--text-primary);
            margin-bottom: 32px;
            font-weight: 600;
            letter-spacing: -2px;
        }

        .hero p {
            font-size: 1.25rem;
            color: var(--text-secondary);
            max-width: 680px;
            margin: 0 auto 48px;
            font-weight: 300;
        }

        .btn-primary {
            background: var(--text-primary);
            color: #fff;
            padding: 18px 42px;
            font-size: 1rem;
            border-radius: 4px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            display: inline-block;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        .btn-primary:hover {
            transform: translateY(-2px);
            background: #000;
            box-shadow: 0 15px 30px rgba(0,0,0,0.15);
        }

        /* --- 6. CONTAINER & GRID --- */
        .container {
            max-width: var(--container-width);
            margin: 0 auto;
            padding: 80px 20px;
            width: 100%;
        }

        .section-header {
            margin-bottom: 60px;
            padding-top: 40px;
            border-top: 1px solid rgba(0,0,0,0.05);
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            flex-wrap: wrap;
            gap: 20px;
        }

        .section-header h2 {
            font-family: var(--font-serif);
            font-size: 2.5rem;
            color: var(--text-primary);
            margin: 0;
        }
        .section-header p {
            color: var(--text-secondary);
            max-width: 400px;
            text-align: right;
            margin: 0;
        }

        .grid-3 {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
        }

        /* --- 7. CARDS --- */
        .card {
            background: var(--bg-card);
            padding: 50px 40px;
            border-radius: 2px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.03);
            transition: all 0.5s ease;
            position: relative;
            top: 0;
            border: 1px solid rgba(0,0,0,0.03);
        }
        .card:hover {
            top: -10px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.06);
        }
        .card-icon { font-size: 2rem; color: var(--gold-accent); margin-bottom: 24px; display: block; }
        .card h3 { font-family: var(--font-serif); font-size: 1.75rem; margin-bottom: 16px; color: var(--text-primary); }
        .card p { color: var(--text-secondary); font-size: 1rem; line-height: 1.7; margin-bottom: 24px; }
        .card-link { font-size: 0.9rem; font-weight: 600; color: var(--text-primary); text-decoration: none; border-bottom: 1px solid var(--gold-accent); padding-bottom: 2px; }

        /* --- 8. EDITORIAL SECTION --- */
        .editorial-section {
            background: #fff;
            padding: 100px 0;
            margin-top: 80px;
        }
        
        .editorial-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 60px;
        }

        .article-card {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            padding-bottom: 40px;
            border-bottom: 1px solid rgba(0,0,0,0.1);
            transition: all 0.3s ease;
            cursor: pointer;
        }
        .article-card:hover { transform: translateY(-5px); }
        .article-card:hover h3 { color: var(--gold-accent); }
        
        .article-meta { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 2px; color: var(--text-light); margin-bottom: 16px; font-weight: 600; }
        .article-card h3 { font-family: var(--font-serif); font-size: 1.8rem; line-height: 1.25; margin-bottom: 15px; color: var(--text-primary); transition: color 0.3s; }
        .article-card p { color: var(--text-secondary); font-size: 1rem; line-height: 1.7; margin-bottom: 20px; max-width: 95%; }
        .read-more { font-size: 0.8rem; font-weight: 700; text-transform: uppercase; letter-spacing: 1px; color: var(--text-primary); border-bottom: 1px solid var(--gold-accent); padding-bottom: 2px; }

        /* --- 9. FORM SECTION --- */
        .form-section { padding: 80px 0; }
        .form-wrapper { max-width: 700px; margin: 0 auto; }
        
        .clean-input-group { margin-bottom: 40px; position: relative; }
        .clean-input { width: 100%; border: none; border-bottom: 1px solid #d1d5db; padding: 15px 0; font-size: 1.2rem; font-family: var(--font-serif); color: var(--text-primary); background: transparent; transition: 0.3s; border-radius: 0; }
        .clean-input:focus { outline: none; border-bottom-color: var(--text-primary); }
        .clean-input::placeholder { color: transparent; }
        .clean-label { position: absolute; top: 15px; left: 0; font-size: 1rem; color: var(--text-light); transition: 0.3s ease; pointer-events: none; }
        .clean-input:focus ~ .clean-label, .clean-input:not(:placeholder-shown) ~ .clean-label { top: -20px; font-size: 0.8rem; color: var(--gold-accent); letter-spacing: 1px; text-transform: uppercase; }

        /* --- 10. FOOTER (AKQA STYLE) --- */
        footer {
            background: #1a1a1a;
            border-top: 1px solid #333;
            padding: 80px 0 40px;
            margin-top: 80px;
            color: #e5e5e5;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 40px;
            max-width: var(--container-width);
            margin: 0 auto;
            padding: 0 20px;
        }

        .footer-brand {
            font-family: var(--font-serif);
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            display: block;
            color: #ffffff;
        }

        .footer-col h4 {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 20px;
            color: #ffffff;
        }

        .footer-col a {
            display: block;
            color: #cccccc;
            margin-bottom: 10px;
            text-decoration: none;
            transition: 0.3s ease;
        }

        .footer-col a:hover {
            color: #ffffff;
        }

        /* --- RESPONSIVE --- */
        @media (max-width: 900px) {
            .hero h1 { font-size: 3.5rem; }
            .section-header { flex-direction: column; align-items: flex-start; text-align: left; }
            .section-header p { text-align: left; }
            .grid-3, .editorial-grid { grid-template-columns: 1fr; }
            .footer-grid { grid-template-columns: 1fr; }
            header { padding: 0 20px; }
            nav { display: none; }
        }
    </style>
</head>
<body>

    <div class="fluid-bg"></div>

    <header id="navbar">
        <div class="brand">GLOBALIS</div>
        <nav>
            <a href="#">Expertise</a>
            <a href="#">Consulting</a>
            <a href="#">Global Reach</a>
            <a href="#">Portal</a>
        </nav>
        <button class="btn-header">
            Get Started
        </button>
    </header>

    <main>
        <section class="hero reveal">
            <span class="eyebrow">Established 2025 • Global Advisory</span>
            <h1>Legitimacy in a<br>Borderless World.</h1>
            <p>Globalis creates the compliant bridge between elite global talent, forward-thinking employers, and international investors.</p>
            <a href="#" class="btn-primary">Begin Application Process</a>
        </section>

        <div class="container">
            <div class="section-header reveal">
                <h2>Our Methodology.</h2>
                <p>A unified ecosystem for the global market, ensuring compliance at every handshake.</p>
            </div>

            <div class="grid-3">
                <div class="card reveal">
                    <span class="card-icon">✦</span>
                    <h3>The Talent</h3>
                    <p>Seasonal or long-term opportunities with vetted global employers. Secure processing with unique ID tracking and transparent auditing.</p>
                    <a href="#" class="card-link">Learn More</a>
                </div>
                <div class="card reveal">
                    <span class="card-icon">❖</span>
                    <h3>The Employer</h3>
                    <p>Access to pre-screened candidates with comprehensive documentation. We eliminate the friction of cross-border hiring.</p>
                    <a href="#" class="card-link">Corporate Solutions</a>
                </div>
                <div class="card reveal">
                    <span class="card-icon">⚖</span>
                    <h3>The Investor</h3>
                    <p>Strategic consulting for company formation, asset protection, and international workforce deployment strategies.</p>
                    <a href="#" class="card-link">Advisory Board</a>
                </div>
            </div>
        </div>

        <section class="editorial-section">
            <div class="container reveal">
                <div class="section-header">
                    <h2>Strategic Insights.</h2>
                    <p>Expert analysis on global mobility trends, compliance frameworks, and cross-border capital flow.</p>
                </div>

                <div class="editorial-grid">
                    <article class="article-card">
                        <span class="article-meta">Global Mobility • 2025 Report</span>
                        <h3>Navigating the New Era of Cross-Border Workforce Compliance.</h3>
                        <p>As international borders become digital gateways, the legal framework for employment is shifting. Our team analyzes the impact of new GDPR standards.</p>
                        <span class="read-more">Read Analysis</span>
                    </article>

                    <article class="article-card">
                        <span class="article-meta">Corporate Advisory • Investment</span>
                        <h3>Structuring Foreign Direct Investment for Long-Term Stability.</h3>
                        <p>How institutional investors are leveraging new bilateral trade agreements to secure assets and optimize tax liabilities in high-growth markets.</p>
                        <span class="read-more">Read Analysis</span>
                    </article>

                    <article class="article-card">
                        <span class="article-meta">Infrastructure • Strategy</span>
                        <h3>The Architecture of Ethical Recruitment.</h3>
                        <p>Why transparency is the new currency. Exploring how blockchain-verified contracts are eliminating fraud for multinational corporations.</p>
                        <span class="read-more">Read Analysis</span>
                    </article>

                    <article class="article-card">
                        <span class="article-meta">Regulatory • Updates</span>
                        <h3>2025 Seasonal Visa Protocols: A Strategic Overview.</h3>
                        <p>Key changes in agricultural and hospitality visa quotas across the EU and North America. What employers need to know to prevent operational bottlenecks.</p>
                        <span class="read-more">Read Analysis</span>
                    </article>
                </div>
            </div>
        </section>

        <section id="form" class="form-section reveal">
            <div class="form-wrapper">
                <div style="text-align: center; margin-bottom: 60px;">
                    <span class="eyebrow">Secure Portal</span>
                    <h2 style="font-family: var(--font-serif); font-size: 3rem; margin: 10px 0;">Initiate Request</h2>
                    <p style="color: var(--text-secondary);">Your application is encrypted and confidential.</p>
                </div>

                <form>
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 40px;">
                        <div class="clean-input-group">
                            <input type="text" class="clean-input" placeholder=" " required>
                            <label class="clean-label">Full Legal Name</label>
                        </div>
                        <div class="clean-input-group">
                            <input type="text" class="clean-input" placeholder=" " required>
                            <label class="clean-label">Nationality</label>
                        </div>
                    </div>

                    <div class="clean-input-group">
                        <input type="email" class="clean-input" placeholder=" " required>
                        <label class="clean-label">Corporate Email Address</label>
                    </div>

                    <div class="clean-input-group">
                        <input type="text" class="clean-input" placeholder=" " required>
                        <label class="clean-label">Primary Objective</label>
                    </div>

                    <button type="button" class="btn-primary" style="width: 100%; margin-top: 20px;">
                        Submit for Review
                    </button>
                </form>
            </div>
        </section>
    </main>

    <footer>
        <div class="footer-grid">
            <div class="footer-col">
                <span class="footer-brand">GLOBALIS</span>
                <p style="font-size: 0.9rem; line-height: 1.6;">
                    Setting the global standard for workforce compliance and consulting excellence.
                </p>
            </div>
            <div class="footer-col">
                <h4>Company</h4>
                <a href="#">About</a>
                <a href="#">Leadership</a>
                <a href="#">Careers</a>
            </div>
            <div class="footer-col">
                <h4>Legal</h4>
                <a href="#">Compliance</a>
                <a href="#">Privacy Policy</a>
                <a href="#">Terms of Service</a>
            </div>
            <div class="footer-col">
                <h4>Connect</h4>
                <a href="mailto:support@example.com">support@example.com</a>
                <a href="#">Global Offices • Worldwide</a>
            </div>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const reveals = document.querySelectorAll('.reveal');
            const revealOnScroll = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('active');
                    }
                });
            }, { threshold: 0.1 });

            reveals.forEach(reveal => revealOnScroll.observe(reveal));

            const header = document.getElementById('navbar');
            window.addEventListener('scroll', () => {
                header.classList.toggle('scrolled', window.scrollY > 50);
            });
        });
    </script>
</body>
</html>